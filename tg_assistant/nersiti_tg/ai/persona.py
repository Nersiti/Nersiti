"""Редактируемая персона/память (data/persona.md).

Контракт:
    get_persona() -> str
    update_persona(text) -> None
    append_memory(note) -> None
TODO(исполнитель): персона идёт в system-prompt (см. ai.reply).
"""
# TODO
