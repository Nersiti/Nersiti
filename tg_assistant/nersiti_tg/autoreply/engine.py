"""Ядро автоответа. ТОЧНО по требованию пользователя:

- OFF/выключен тумблер -> ничего.
- DRAFT (кнопочный) -> сгенерировать (preset или make_reply), положить в
  drafts:pending, НЕ отправлять (ждём подтверждения в панели).
- AUTO (тумблер) -> отправить сразу:
    PRESET   -> заготовленный текст;
    GENERATE -> сгенерировать ответ;
    DIALOGUE -> генерация с полным контекстом+памятью («ведёт диалог»).

Контракт (async):
    async handle(client, event, incoming_text, chat_id) -> None
TODO(исполнитель): по docs/PLAN.md §8.
"""
# TODO
