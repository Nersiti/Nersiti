"""Отправка сообщений: имитация набора, задержки, отправка черновиков.

Контракт (async):
    async send_reply(client, chat_id, text, reply_to=None, simulate_typing=True)
    async send_draft_now(client, draft_id)
TODO(исполнитель): по docs/PLAN.md §6. Задержки из autoreply.min/max_delay_sec.
"""
# TODO
