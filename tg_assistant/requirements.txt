# --- Telegram (userbot/десктоп-клиент, MTProto) ---
telethon==1.36.0

# --- Десктоп-приложение (Windows, строгая чёрная тема) ---
PySide6==6.7.2

# --- Веб-панель ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9

# --- Конфиг/утилиты ---
pydantic==2.9.2
pydantic-settings==2.5.2
pyyaml==6.0.2
httpx==0.27.2

# --- Опционально: семантический поиск / RAG ---
# sqlite-vec==0.1.6
# или chromadb==0.5.5

# Ollama ставится отдельно как приложение (https://ollama.com),
# в requirements не входит. Модель: `ollama pull qwen2.5:7b-instruct`.

# --- Доступ в интернет для модели (инструменты) ---
ddgs==6.1.7                # поиск DuckDuckGo без ключа (search_provider: duckduckgo)
trafilatura==1.12.2        # извлечение основного текста со страниц (web_fetch)
# tavily-python==0.5.0     # если выбран search_provider: tavily
# SearXNG ставится отдельно как локальный сервис (privacy-friendly поиск)

# связка asyncio(Telethon) с Qt в GUI
qasync==0.27.1
